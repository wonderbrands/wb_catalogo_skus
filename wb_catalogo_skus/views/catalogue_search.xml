<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_product_template_search_wb" model="ir.ui.view">
        <field name="name">product.template.search.wb.multicaja</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view"/>
        <field name="arch" type="xml">
            
            <xpath expr="//filter[@name='filter_to_sell']" position="after">
                <separator/>
                <filter string="SKUs de Cajas (#BOX)" 
                        name="filter_is_box" 
                        domain="[('default_code', 'ilike', '#BOX')]"/>
                
                <filter string="Ocultar Cajas (#BOX)" 
                        name="filter_no_box" 
                        domain="[('default_code', 'not ilike', '#BOX')]"/>
                <separator/>
            </xpath>

        </field>
    </record>
</odoo>